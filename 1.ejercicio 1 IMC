"""indice de masa corporal es: Peso (KG) / Altura m2 """

peso = float(input("ingrese su peso: "))
altura = float(input("ingrese su altura:"))
imc = peso / (altura ** 2)
print(f"tu indice de masa corporal  es de: {imc}")

